import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
    fonts: {
        heading: `'Imperial Script', sans-serif`,
        body: `'Hina Mincho', serif`,
    },
    heading: {
        fontWeight: 'normal'
    },
})  